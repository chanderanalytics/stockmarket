"use client";

import * as React from "react";
import { TopBar } from "./top-bar";

// AppHeader composes the top bar (and any future header regions).
export function AppHeader() {
  return <TopBar />;
}
